#import "../fragments/full-goal-suggestion-template.graphql"

mutation goalSuggestionTemplateCreate($title: String!) {
  goalSuggestionTemplateCreate(input: { title: $title }) {
    ...FullGoalSuggestionTemplate
  }
}
