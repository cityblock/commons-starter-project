#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithUrgentTasks($pageNumber: Int!, $pageSize: Int!) {
  patientsWithUrgentTasks(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
