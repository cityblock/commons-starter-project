#import "../fragments/full-phone.graphql"

mutation phoneCreate($phoneNumber: String!, $type: PhoneTypeOptions!, $description: String) {
  phoneCreate(input: { phoneNumber: $phoneNumber, type: $type, description: $description }) {
    ...FullPhone
  }
}
