#import "../fragments/full-goal-suggestion-template.graphql"

mutation goalSuggestionTemplateDelete($goalSuggestionTemplateId: ID!) {
  goalSuggestionTemplateDelete(input: { goalSuggestionTemplateId: $goalSuggestionTemplateId }) {
    ...FullGoalSuggestionTemplate
  }
}
