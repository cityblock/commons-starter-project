#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsNewToCareTeam($pageNumber: Int!, $pageSize: Int!) {
  patientsNewToCareTeam(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}