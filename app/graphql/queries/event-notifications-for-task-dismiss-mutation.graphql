#import "../fragments/full-event-notification.graphql"

mutation eventNotificationsForTaskDismiss($taskId: ID!) {
  eventNotificationsForTaskDismiss(input: { taskId: $taskId }) {
    ...FullEventNotification
  }
}
