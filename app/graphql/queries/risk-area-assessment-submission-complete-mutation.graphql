#import "../fragments/full-risk-area-assessment-submission.graphql"

mutation riskAreaAssessmentSubmissionComplete($riskAreaAssessmentSubmissionId: ID!) {
  riskAreaAssessmentSubmissionComplete(
    input: { riskAreaAssessmentSubmissionId: $riskAreaAssessmentSubmissionId }
  ) {
    ...FullRiskAreaAssessmentSubmission
  }
}
