#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithMissingInfo($pageNumber: Int!, $pageSize: Int!) {
  patientsWithMissingInfo(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
