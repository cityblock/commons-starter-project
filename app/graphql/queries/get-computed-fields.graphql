#import "../fragments/full-computed-field.graphql"

query getComputedFields($orderBy: ComputedFieldOrderOptions, $availableOnly: Boolean) {
  computedFields(orderBy: $orderBy, availableOnly: $availableOnly) {
    ...FullComputedField
  }
}
