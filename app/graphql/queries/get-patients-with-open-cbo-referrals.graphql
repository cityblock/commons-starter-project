#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithOpenCBOReferrals($pageNumber: Int!, $pageSize: Int!) {
  patientsWithOpenCBOReferrals(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
