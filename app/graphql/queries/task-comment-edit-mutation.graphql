#import "../fragments/full-task-comment.graphql"

mutation taskCommentEdit($taskCommentId: String!, $body: String!) {
  taskCommentEdit(input: { taskCommentId: $taskCommentId, body: $body }) {
    ...FullTaskComment
  }
}
