#import "../fragments/full-screening-tool-score-range.graphql"

mutation screeningToolScoreRangeDelete($screeningToolScoreRangeId: String!) {
  screeningToolScoreRangeDelete(input: { screeningToolScoreRangeId: $screeningToolScoreRangeId }) {
    ...FullScreeningToolScoreRange
  }
}
