#import "../fragments/full-patient-external-provider.graphql"

mutation patientExternalProviderDelete($patientExternalProviderId: ID!) {
  patientExternalProviderDelete(input: { patientExternalProviderId: $patientExternalProviderId }) {
    ...FullPatientExternalProvider
  }
}
