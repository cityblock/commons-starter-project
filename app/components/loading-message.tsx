import * as React from 'react';
import * as styles from '../css/components/loading-message.css';

export const LoadingMessage: React.StatelessComponent<{}> = () => (
  <div className={styles.heading}>Loading...</div>
);
