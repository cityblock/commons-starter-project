#import "../fragments/full-address.graphql"

mutation addressEdit(
  $addressId: ID!
  $patientId: ID!
  $zip: String
  $street: String
  $city: String
  $state: String
  $description: String
) {
  addressEdit(
    input: {
      addressId: $addressId
      patientId: $patientId
      zip: $zip
      street: $street
      city: $city
      state: $state
      description: $description
    }
  ) {
    ...FullAddress
  }
}
