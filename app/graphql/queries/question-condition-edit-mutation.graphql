#import "../fragments/full-question-condition.graphql"

mutation questionConditionEdit($questionConditionId: ID!, $answerId: ID!, $questionId: ID!) {
  questionConditionEdit(
    input: {
      questionConditionId: $questionConditionId
      answerId: $answerId
      questionId: $questionId
    }
  ) {
    ...FullQuestionCondition
  }
}
