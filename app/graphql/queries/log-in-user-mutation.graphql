#import "../fragments/full-user.graphql"

mutation logInUser($googleAuthCode: String!) {
  userLogin(input: { googleAuthCode: $googleAuthCode }) {
    authToken
    user {
      ...FullUser
    }
  }
}
