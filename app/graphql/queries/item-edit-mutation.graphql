#import "../fragments/base-item.graphql"

mutation itemCreate(
  $pokemonId: ID!
  $name: String!
  $price: Int!
  $happiness: Int!
  $imageUrl: String!
) {
  itemCreate(
    input: {
      pokemonId: $pokemonId
      name: $name
      price: $price
      happiness: $happiness
      imageUrl: $imageUrl
    }
  ) {
    ...BaseItem
  }
}
