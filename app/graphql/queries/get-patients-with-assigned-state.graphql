#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithAssignedState($pageNumber: Int!, $pageSize: Int!) {
  patientsWithAssignedState(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
