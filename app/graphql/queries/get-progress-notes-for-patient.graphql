#import "../fragments/full-progress-note.graphql"

query getProgressNotesForPatient($patientId: ID!, $completed: Boolean!, $glassBreakId: ID) {
  progressNotesForPatient(
    patientId: $patientId
    completed: $completed
    glassBreakId: $glassBreakId
  ) {
    ...FullProgressNote
  }
}
