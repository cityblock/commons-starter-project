#import "../fragments/full-risk-area-group-for-patient.graphql"

query getRiskAreaGroupsForPatient($patientId: ID!, $glassBreakId: ID) {
  riskAreaGroupsForPatient(patientId: $patientId, glassBreakId: $glassBreakId) {
    ...FullRiskAreaGroupForPatient
  }
}
