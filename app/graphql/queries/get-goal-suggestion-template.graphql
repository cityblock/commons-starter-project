#import "../fragments/full-goal-suggestion-template.graphql"

query getGoalSuggestionTemplate($goalSuggestionTemplateId: String!) {
  goalSuggestionTemplate(goalSuggestionTemplateId: $goalSuggestionTemplateId) {
    ...FullGoalSuggestionTemplate
  }
}
