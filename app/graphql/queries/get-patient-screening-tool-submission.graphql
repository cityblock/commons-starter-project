#import "../fragments/full-patient-screening-tool-submission.graphql"

query getPatientScreeningToolSubmission($patientScreeningToolSubmissionId: String!) {
  patientScreeningToolSubmission(
    patientScreeningToolSubmissionId: $patientScreeningToolSubmissionId
  ) {
    ...FullPatientScreeningToolSubmission
  }
}
