#import "../fragments/full-computed-patient-status.graphql"

query getPatientComputedPatientStatus($patientId: ID!) {
  patientComputedPatientStatus(patientId: $patientId) {
    ...FullComputedPatientStatus
  }
}
