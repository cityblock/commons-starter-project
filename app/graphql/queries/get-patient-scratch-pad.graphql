#import "../fragments/full-patient-scratch-pad.graphql"

query getPatientScratchPad($patientId: ID!, $glassBreakId: ID) {
  patientScratchPad(patientId: $patientId, glassBreakId: $glassBreakId) {
    ...FullPatientScratchPad
  }
}
