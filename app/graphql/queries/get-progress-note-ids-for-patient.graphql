query getProgressNoteIdsForPatient($patientId: ID!, $completed: Boolean!, $glassBreakId: ID) {
  progressNoteIdsForPatient(
    patientId: $patientId
    completed: $completed
    glassBreakId: $glassBreakId
  )
}
