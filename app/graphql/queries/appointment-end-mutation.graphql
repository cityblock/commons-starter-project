mutation appointmentEnd($patientId: String!, $appointmentId: String!, $appointmentNote: String!) {
  appointmentEnd(input: {
    patientId: $patientId,
    appointmentId: $appointmentId,
    appointmentNote: $appointmentNote,
  }) {
    success
  }
}
