#import "../fragments/full-patient-screening-tool-submission.graphql"

mutation patientScreeningToolSubmissionScore($patientScreeningToolSubmissionId: ID!) {
  patientScreeningToolSubmissionScore(
    input: { patientScreeningToolSubmissionId: $patientScreeningToolSubmissionId }
  ) {
    ...FullPatientScreeningToolSubmission
  }
}
