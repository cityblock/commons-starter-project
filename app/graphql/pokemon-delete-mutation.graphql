mutation deletePokemon($id: ID!) {
  deletedPokemon(id: $id) {
    id
    pokemonNumber
    name
    attack
    defense
    pokeType
    moves
    imageUrl
    createdAt
    updatedAt
    deletedAt
  }
}
