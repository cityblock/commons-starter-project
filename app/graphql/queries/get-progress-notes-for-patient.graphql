#import "../fragments/full-progress-note.graphql"

query getProgressNotesForPatient($patientId: ID!, $completed: Boolean!) {
  progressNotesForPatient(patientId: $patientId, completed: $completed) {
    ...FullProgressNote
  }
}
