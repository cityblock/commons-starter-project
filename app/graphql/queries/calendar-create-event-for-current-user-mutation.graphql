mutation calendarCreateEventForCurrentUser(
  $startDatetime: String!
  $endDatetime: String!
  $inviteeEmails: [String!]!
  $location: String!
  $title: String!
  $reason: String!
) {
  calendarCreateEventForCurrentUser(
    input: {
      startDatetime: $startDatetime
      endDatetime: $endDatetime
      inviteeEmails: $inviteeEmails
      location: $location
      title: $title
      reason: $reason
    }
  ) {
    eventCreateUrl
  }
}
