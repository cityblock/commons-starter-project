#import "../fragments/full-concern.graphql"

mutation concernSuggestionCreate(
  $answerId: String!
  $concernId: String!
) {
  concernSuggestionCreate(input: {
    answerId: $answerId
    concernId: $concernId
  }) {
    ...FullConcern
  }
}
