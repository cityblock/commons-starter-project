#import "../fragments/full-goal-suggestion-template.graphql"

query getGoalSuggestionTemplates($orderBy: GoalSuggestionOrderOptions) {
  goalSuggestionTemplates(orderBy: $orderBy) {
    ...FullGoalSuggestionTemplate
  }
}
