#import "../fragments/full-task-comment.graphql"

mutation taskCommentCreate($taskId: String!, $body: String!) {
  taskCommentCreate(input: { taskId: $taskId, body: $body }) {
    ...FullTaskComment
  }
}
