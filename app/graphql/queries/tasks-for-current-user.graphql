#import "../fragments/full-task.graphql"

query getTasksForCurrentUser($pageNumber: Int, $pageSize: Int, $orderBy: TaskOrderOptions) {
  tasksForCurrentUser(pageNumber: $pageNumber, pageSize: $pageSize, orderBy: $orderBy) {
    edges {
      node {
        ...FullTask
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
  }
}
