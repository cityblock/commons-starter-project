query getSinglePokemon($pokemonId: ID!) {
  fullPokemon(pokemonId: $pokemonId) {
    id
    pokemonNumber
    name
    attack
    defense
    pokeType
    moves
    imageUrl
    createdAt
    updatedAt
    deletedAt
    items {
      id
      name
      imageUrl
    }
  }
}
