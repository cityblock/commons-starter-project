#import "../fragments/full-care-plan-suggestion-for-patient.graphql"

query getCarePlanSuggestionsFromScreeningToolsForPatient($patientId: ID!, $glassBreakId: ID) {
  carePlanSuggestionsFromScreeningToolsForPatient(
    patientId: $patientId
    glassBreakId: $glassBreakId
  ) {
    ...FullCarePlanSuggestionForPatient
  }
}
