#import "./full-user.graphql"
#import "./full-task.graphql"
#import "./full-task-comment.graphql"

fragment FullTaskEvent on TaskEvent {
  id
  taskId
  task {
    ...FullTask
  }
  userId
  user {
    ...FullUser
  }
  eventType
  eventCommentId
  eventComment {
    ...FullTaskComment
  }
  eventUserId
  eventUser {
    ...FullUser
  }
  createdAt
  updatedAt
  deletedAt
}
