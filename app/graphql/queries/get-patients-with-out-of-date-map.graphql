#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithOutOfDateMAP($pageNumber: Int!, $pageSize: Int!) {
  patientsWithOutOfDateMAP(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
