#import "../fragments/short-patient.graphql"

query getPatientPanel($pageNumber: Int!, $pageSize: Int!, $filters: PatientFilterOptions!) {
  patientPanel(pageNumber: $pageNumber, pageSize: $pageSize, filters: $filters) {
    edges {
      node {
        ...ShortPatient
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
