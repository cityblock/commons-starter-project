#import "../fragments/full-user.graphql"

mutation patientCareTeamAddUser($patientId: ID!, $userId: ID!) {
  careTeamAddUser(input: { patientId: $patientId, userId: $userId }) {
    ...FullUser
  }
}
