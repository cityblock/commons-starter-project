#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithIntakeInProgress($pageNumber: Int!, $pageSize: Int!) {
  patientsWithIntakeInProgress(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
