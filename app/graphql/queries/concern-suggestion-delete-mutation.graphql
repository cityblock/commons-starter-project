#import "../fragments/full-concern.graphql"

mutation concernSuggestionDelete($answerId: String!, $concernId: String!) {
  concernSuggestionDelete(input: { answerId: $answerId, concernId: $concernId }) {
    ...FullConcern
  }
}
