#import "../fragments/full-email.graphql"

mutation emailCreate($emailAddress: String!, $description: String) {
  emailCreate(input: { emailAddress: $emailAddress, description: $description }) {
    ...FullEmail
  }
}
