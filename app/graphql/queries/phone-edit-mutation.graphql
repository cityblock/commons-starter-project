#import "../fragments/full-phone.graphql"

mutation phoneEdit($phoneId: ID!, $patientId: ID!, $phoneNumber: String!) {
  phoneEdit(input: { phoneId: $phoneId, patientId: $patientId, phoneNumber: $phoneNumber }) {
    ...FullPhone
  }
}
