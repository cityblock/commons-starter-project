#import "../fragments/full-question.graphql"

mutation questionCreate(
  $title: String!
  $answerType: AnswerTypeOptions!
  $validatedSource: String
  $riskAreaId: ID
  $screeningToolId: ID
  $progressNoteTemplateId: ID
  $order: Int!
  $applicableIfType: QuestionConditionTypeOptions
  $computedFieldId: ID
) {
  questionCreate(
    input: {
      title: $title
      answerType: $answerType
      validatedSource: $validatedSource
      riskAreaId: $riskAreaId
      screeningToolId: $screeningToolId
      progressNoteTemplateId: $progressNoteTemplateId
      order: $order
      applicableIfType: $applicableIfType
      computedFieldId: $computedFieldId
    }
  ) {
    ...FullQuestion
  }
}
