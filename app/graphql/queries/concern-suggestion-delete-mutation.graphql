#import "../fragments/full-concern.graphql"

mutation concernSuggestionDelete($answerId: ID, $screeningToolScoreRangeId: ID, $concernId: ID!) {
  concernSuggestionDelete(
    input: {
      answerId: $answerId
      screeningToolScoreRangeId: $screeningToolScoreRangeId
      concernId: $concernId
    }
  ) {
    ...FullConcern
  }
}
