mutation pokemonDelete($pokemonId: ID!) {
  pokemonDelete(input: { pokemonId: $pokemonId }) {
    id
    pokemonNumber
    name
    attack
    defense
    pokeType
    moves
    imageUrl
    createdAt
    updatedAt
    deletedAt
    items {
      id
      name
      imageUrl
    }
  }
}
