#import "../fragments/full-task-template.graphql"

mutation taskTemplateDelete($taskTemplateId: String!) {
  taskTemplateDelete(input: { taskTemplateId: $taskTemplateId }) {
    ...FullTaskTemplate
  }
}
