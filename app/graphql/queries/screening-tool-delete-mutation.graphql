#import "../fragments/full-screening-tool.graphql"

mutation screeningToolDelete($screeningToolId: ID!) {
  screeningToolDelete(input: { screeningToolId: $screeningToolId }) {
    ...FullScreeningTool
  }
}
