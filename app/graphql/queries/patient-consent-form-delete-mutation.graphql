#import "../fragments/full-patient-consent-form.graphql"

mutation patientConsentFormDelete($patientConsentFormId: ID!) {
  patientConsentFormDelete(input: { patientConsentFormId: $patientConsentFormId }) {
    ...FullPatientConsentForm
  }
}
