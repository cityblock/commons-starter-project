#import "../fragments/full-patient-for-dashboard.graphql"

query getPatientsWithPendingSuggestions($pageNumber: Int!, $pageSize: Int!) {
  patientsWithPendingSuggestions(pageNumber: $pageNumber, pageSize: $pageSize) {
    edges {
      node {
        ...FullPatientForDashboard
      }
    }
    pageInfo {
      hasPreviousPage
      hasNextPage
    }
    totalCount
  }
}
