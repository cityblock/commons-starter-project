#import "../fragments/full-progress-note.graphql"

mutation progressNoteCompleteSupervisorReview($progressNoteId: ID!) {
  progressNoteCompleteSupervisorReview(input: { progressNoteId: $progressNoteId }) {
    ...FullProgressNote
  }
}
