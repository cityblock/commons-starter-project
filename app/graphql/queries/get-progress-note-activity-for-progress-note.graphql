#import "../fragments/full-progress-note-activity.graphql"

query getProgressNoteActivityForProgressNote($progressNoteId: String!) {
  progressNoteActivityForProgressNote(progressNoteId: $progressNoteId) {
    ...FullProgressNoteActivity
  }
}
