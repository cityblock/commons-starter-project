#import "../fragments/full-concern.graphql"

mutation concernSuggestionDelete(
  $answerId: String
  $screeningToolScoreRangeId: String
  $concernId: String!
) {
  concernSuggestionDelete(
    input: {
      answerId: $answerId
      screeningToolScoreRangeId: $screeningToolScoreRangeId
      concernId: $concernId
    }
  ) {
    ...FullConcern
  }
}
