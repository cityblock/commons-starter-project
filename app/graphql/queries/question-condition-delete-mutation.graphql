#import "../fragments/full-question-condition.graphql"

mutation questionConditionDelete($questionConditionId: String!) {
  questionConditionDelete(input: { questionConditionId: $questionConditionId }) {
    ...FullQuestionCondition
  }
}
