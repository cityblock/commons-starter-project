#import "../fragments/full-risk-area-assessment-submission.graphql"

query getRiskAreaAssessmentSubmissionForPatient(
  $riskAreaId: ID!
  $patientId: ID!
  $completed: Boolean!
) {
  riskAreaAssessmentSubmissionForPatient(
    riskAreaId: $riskAreaId
    patientId: $patientId
    completed: $completed
  ) {
    ...FullRiskAreaAssessmentSubmission
  }
}
