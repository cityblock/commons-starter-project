#import "../fragments/full-patient-concern.graphql"
#import "../fragments/full-patient-goal.graphql"

query getPatientCarePlan($patientId: ID!) {
  carePlanForPatient(patientId: $patientId) {
    concerns {
      ...FullPatientConcern
    }
    goals {
      ...FullPatientGoal
    }
  }
}
