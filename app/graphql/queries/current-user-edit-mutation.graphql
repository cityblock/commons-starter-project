#import "../fragments/full-user.graphql"

mutation currentUserEdit($firstName: String, $lastName: String, $locale: String) {
  currentUserEdit(input: { firstName: $firstName, lastName: $lastName, locale: $locale }) {
    ...FullUser
  }
}
