#import "../fragments/full-patient-medication.graphql"

query getPatientMedications($patientId: ID!) {
  patientMedications(patientId: $patientId) {
    medications {
      active {
        ...FullPatientMedication
      }
    }
  }
}
